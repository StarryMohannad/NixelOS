#!/bin/sh

FileDate="$(date '+%Y%m%d%H%M%S')"

setsid -f mpv --no-video ~/.local/nf/files/sounds/menu.open.mp3

if [ "$1" == "rectangle" ] ; then
	shotgun -f png -g "$(hacksaw)" ~/Sync/Pictures/Screenshots/"$FileDate".png
elif [ "$1" == "fullscreen" ] ; then
	shotgun -f png -s ~/Sync/Pictures/Screenshots/"$FileDate".png
else
    echo "No File Type specified"
    return 1
fi

xclip -selection clipboard -t image/png -i ~/Sync/Pictures/Screenshots/"$FileDate".png
setsid -f mpv --no-video ~/.local/nf/files/sounds/screenshot.mp3
notify-send "Screenshot taken" "'$FileDate'.png is now in the clipboard"
